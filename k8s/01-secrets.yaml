apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: gravitas
type: Opaque
stringData:
  PGPASSWORD: replace-me
  ADMIN_PASSCODE: replace-me
  SMTP_PASSWORD: replace-me
  # Optional. Read-only Squarespace API key for the admin members section.
  SQUARESPACE_API_KEY: ""
